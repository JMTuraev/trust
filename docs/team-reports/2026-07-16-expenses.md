# 2026-07-16 — EXPENSES (Xarajat) hisoboti

Teammate: EXPENSES. Qamrov: `src/routes/expenses.js`, `src/routes/categories.js`, `src/routes/stt.js`,
`mobile/lib/screens/xarajat.dart` (+ o'qish: store/api/l10n/parse, prototiplar, migratsiyalar).

---

## 1. RANG BUGI — ildiz sabab (aniq tashxis)

**Shikoyat:** yangi xarajat yozilganda summa raqamlari brend rangda bo'lishi kerak
(chiqim = `p.red` 0xFFA94438/0xFFD2695B, kirim = `p.green` 0xFF2F7A54/0xFF4CAF82), lekin "ba'zida xato".

**Dizayn nima deydi:** eski prototip (Trust_STT-off.html) da `Xarajat → color: {{ redC }}`,
`Daromad → {{ greenC }}`; store'dagi eski chat-bubble ham `isD ? green : red`. Yangi papka-UI faylining
o'z sarlavhasi ham "summa yashil/qizil" deydi. Demak NIYAT: summa raqami HAR DOIM yo'nalish brend-rangida.

**Nega "goh ishlaydi, goh yo'q" (uch qatlamli sabab):**

1. **Asosiy sabab — bitta ekranda ikki xil palitra.** Yozish oqimining birinchi yarmi to'g'ri edi
   (input highlight, fly-chip, papka kartasi count-up, oy balansi — hammasi red/green), ikkinchi yarmi
   esa chiqimga `p.ink` (qora/oq!) ishlatgan:
   - `xarajat.dart` ~700: papka tafsiloti JAMI summasi — `xfDInc ? p.green : p.ink` ← chiqim INK;
   - ~772: tafsilot ichidagi yozuv qatori — `r['inc'] ? p.green : p.ink` ← chiqim INK;
   - ~903: jurnal (Oxirgi o'zgarishlar) qatori — `o['inc'] ? p.green : p.ink` ← chiqim INK.
   Kirim hamma joyda yashil chiqaveradi, chiqim esa qayeriga qaraganingizga qarab goh qizil, goh qora —
   foydalanuvchiga bu "ba'zida buziladi" bo'lib ko'rinadi. (Shu blokning o'zida ikonka rangi `green:red`
   edi — ya'ni bu dizayn qarori emas, chala ko'chirilgan kod.)
2. **Yozish paytidagi haqiqiy race:** input rangi ikki manbadan — lokal kalit-so'z taxmini (darhol) va
   server `/api/expenses/preview` (550ms debounce + tarmoq). Lokal lug'at server bilan TO'LIQ sinxron
   emas edi: `foyda` serverda kirim-ot, mobilda yo'q; `qaytardi(m)` mobilda kirim-fe'l sifatida
   "qaytardim"ni ham yutib yuborardi. LLM ishlamay qolsa/oflaynda server preview kelmaydi → lokal taxmin
   yakuniy saqlash natijasiga zid rang ko'rsatishi mumkin edi.
3. **Chekka holat:** kirim→chiqim flip (PATCH `{income:false}`, toifasiz) 'Daromad' nomli CHIQIM yozuvini
   qoldirardi — papka yo'nalishi `_xfFolders()` da birinchi yozuvdan aniqlanadi, aralash papka rangi esa
   yozuvlar tartibiga qarab sakraydi (intermittent!).

**Tuzatishlar (bajarildi):**
- `xarajat.dart` 3 nuqtada `p.ink` → `p.red` (izohlar bilan). Endi oqimning HAR bosqichi:
  input → fly-chip → papka count-up → balans → tafsilot jami → tafsilot qatori → jurnal — brend rangda.
  `Colors.red/green` hech qayerda yo'q (faqat theme `p.red/p.green`).
- Lokal heuristika sinxronlandi: `foyda|фойда` kirim-otlarga qo'shildi; `qaytardi\b` so'z chegarasi bilan
  (3-shaxs = kirim), `qaytardim|qaytarib berdim` chiqim-fe'llarga.
- Backend PATCH guard: `{income:false}` + toifasiz + eski toifa 'Daromad' → `Boshqa` (aralash papka yo'q).
- Server tomonidagi qoida-lug'atni mobil bilan to'liq tenglashtirish — parse.js patch TAKLIFI (§4,
  parse.js men uchun read-only).

## 2. YANGI FUNKSIYA — papkani qo'lda tahrirlash (XOTIRA §4)

Backend (`categories.js`):
- `GET /api/categories?all=1` — arxivlanganlar ham (`archived` flag bilan); default o'zgarmagan (faollar).
- `PATCH /api/categories/:id { name }` — **qayta nomlash KASKADI**: `expenses.category` (faqat chiqimlar)
  va `word_map` lug'ati (hits birlashtirilib) yangi nomga ko'chadi — papka tarixi bo'linmaydi.
- Guardlar: «Boshqa» nomini o'zgartirish/arxivlash/band qilish → 400 (parser zaxira papkasi);
  duplicate nom → 409 aniq xabar bilan. O'chirish endpointi YO'Q (dizayn bo'yicha).

Backend (`expenses.js`):
- `GET /api/expenses?category=&limit=` ixtiyoriy filtri (limit 1..1000 clamp) — papka ichi so'rovi,
  E2E va kelgusi sahifalash uchun; default xatti-harakat o'zgarmagan.
- Income-flip guard (§1.3).

Mobil (`xarajat.dart`, prototip uslubida — mavjud confirm-karta/chip tiliga 1:1 mos):
- **Papka kartasini uzoq bosish** → pastdan "PAPKANI TAHRIRLASH" kartasi: nom o'zgartirish (inline pill
  input + OK), Arxivlash/Arxivdan qaytarish, «Arxivda» badge, holat izohlari. Loading (_PulseDots),
  oflayn/xato toastlari, busy-guard bor. Kirim papkasi ('Daromad') va «Boshqa» — tushuntirish bilan qulf.
- **Tafsilotdagi yozuv qatorini bosish** → "PAPKAGA KO'CHIRISH" kartasi: faol toifa chiplari (joriysidan
  tashqari), tanlash → `PATCH /expenses/:id {category}` → optimistik yangilash + jurnal yozuvi + toast +
  nishon papkada "yutish" pulsi. Kirim yozuvlari ko'chirilmaydi (server baribir 'Daromad'ga majburlaydi).
- Rename/arxiv/ko'chirish jurnalga (Oxirgi o'zgarishlar) tushadi; barcha matnlar `store.L()` orqali
  **l10n-fallback** bilan (kalit yo'q bo'lsa o'zbekcha zaxira — §4 patch qo'llangach 6 tilda).
- Store READ-ONLY sharti buzilmadi: faqat public API (`store.S/set/hydrate/toast_/L/xfEmoji`) ishlatildi.

**Migratsiya kerak emas:** `categories` jadvalida `archived`, `is_base`, `unique(user_id,name)` allaqachon
bor (005), `expenses.category` text. Yangi ustun talab qilinmadi — shuning uchun yangi migratsiya fayli
qo'shilmadi (qoida: faqat kerak bo'lsa).

## 3. E2E TRACE (ovoz → STT → parse → confirm → papka → jurnal)

Tekshirildi (kod bo'ylab to'liq iz):
- STT: mobil `stt.dart` (wav 16k) → `POST /api/stt/transcribe` → Groq whisper → OpenAI zaxira → 503 flag
  o'chiq holatda. Hozir matn-birinchi rejim (kSttEnabled=false) — STT endpoint tegilmadi, ishlaydi.
- Parse kontrakt: `actions[].{direction,amount,currency,category,note,person,new_category_suggestion,
  confidence}` — mobil bilan mos ('direction' nomi to'g'ri ishlatilgan). `amount` server tomonda int
  (Math.round), mobil `_numToInt` string/num ikkalasini ham hazm qiladi (numeric(18,2) PostgREST string
  qaytarsa ham OK).
- **Qarz yo'nalishi yutilmaydi:** LLM/qoida qarz desa → `/confirm` saqlamaydi, `routed[]` qaytaradi →
  mobil toast + «O'tish» → `_routeQarz` hamkor oynasi (topilmasa yangi hamkor + qarzDraft). QARZ_SIGNAL
  deterministik guard ham joyida. E2E skriptda alohida qadam bor.
- Yangi toifa taklifi: suggestion → category='Boshqa' → mobil TRAY → ✨ chip → `accept_new_category` →
  server yaratadi. Ishlaydi.
- Fallback zanjiri: LLM (Groq→OpenAI) → qoida-parser (needs_confirm=true) → mobil server yiqilsa lokal
  `xarParse_` + `POST /expenses` (`_xarOffline`). Har qatlam zaxirali — XOTIRA §1 ga mos.

Topilgan kontrakt nomuvofiqliklar (muhimlari):
1. ~~preview lokal-lug'at divergensiyasi~~ — tuzatildi/patch taklif (§1.2, §4).
2. **needs_confirm mobairda e'tiborsiz:** server past-ishonch/summa-mos-emas deb bersa ham mobil to'g'ridan
   saqlaydi (faqat 'Boshqa'-chiqimlar tray'ga boradi). Yangi papka-UI dizayni buni "undo-toast" bilan
   qoplaydi — ongli drift, lekin XOTIRA §3 dagi tasdiqlash kartasi bilan farqni lead bilsin.
3. **Merge kirim↔chiqim teshigi (store.dart):** «X ni Daromadga birlashtir» — server chiqimlarni 'Daromad'
   toifasiga qo'ymaydi ('Boshqa'ga tushadi), lokal esa 'Daromad' deb ko'rsatadi → keyingi hydrate'da
   sakraydi. Patch taklifi §4.3.
4. `/summary/month` server-lokal oy chegarasi UTC (Render) — mobil lokal (UTC+5) hisoblaydi; mobil bu
   endpointni ishlatmaydi, lekin kelajakda farq beradi (eslatma).
5. XOTIRA §7 "STT_ENABLED default false" deydi, config.js esa default TRUE (`!== 'false'`) — hujjat/kod
   nomuvofiq (2026-07-15 chat-ovoz uchun ongli o'zgartirilgan ko'rinadi). Hujjatni yangilash kerak.

## 4. TAKLIF QILINGAN SHARED PATCHLAR (lead qo'llaydi)

### 4.1 l10n.dart — yangi kalitlar (UI hozir fallback bilan ishlaydi, bu patch 6 tilni to'ldiradi)
Har til lug'atiga (lUz/lRu/lEn/lEs/lFr/lZh oxiriga) quyidagi bloklarni qo'shing:

```dart
// lUz:
'xfEditFolderCap': 'PAPKANI TAHRIRLASH', 'xfRename': 'Nomini o\'zgartirish',
'xfArchive': 'Arxivlash', 'xfUnarchive': 'Arxivdan qaytarish', 'xfArchivedBadge': 'Arxivda',
'xfArchiveHint': 'Arxivlash — o\'chirish emas: tarix saqlanadi, AI taklif qilmaydi',
'xfArchivedHint': 'Arxivda: AI taklif qilmaydi, eski yozuvlar saqlanadi',
'xfBoshqaFixedHint': '«Boshqa» — zaxira papka: aniqlanmagan yozuvlar shu yerga tushadi, tahrirlanmaydi',
'xfFolderNoCatHint': 'Bu papka toifalar ro\'yxatida topilmadi — tahrirlash uchun internetni tekshiring',
'xfMoveCap': 'PAPKAGA KO\'CHIRISH', 'xfMoveFrom': 'Hozir: {cat} — qaysi papkaga o\'tsin?',
'xfMoveNoCats': 'Boshqa faol papka yo\'q — internetni tekshiring yoki yangi toifa oching',
'tIncomeFolderFixed': 'Kirim papkasi tizim tomonidan boshqariladi',
'tFolderNoCat': 'Papka toifasi serverda topilmadi',
'tFolderRenamed': 'Papka nomi yangilandi — yozuvlar birga ko\'chdi',
'tFolderArchived': 'Arxivlandi — AI endi bu papkani taklif qilmaydi, tarix saqlanadi',
'tFolderUnarchived': 'Arxivdan qaytarildi — papka yana taklif qilinadi',
'tEntryNotFound': 'Yozuv topilmadi — yangilab qayta urinib ko\'ring',
'tMovedTo': 'Ko\'chirildi: {cat}', 'logArchived': 'arxivga', 'logUnarchived': 'arxivdan',

// lRu:
'xfEditFolderCap': 'РЕДАКТИРОВАТЬ ПАПКУ', 'xfRename': 'Переименовать',
'xfArchive': 'В архив', 'xfUnarchive': 'Вернуть из архива', 'xfArchivedBadge': 'В архиве',
'xfArchiveHint': 'Архив — не удаление: история сохраняется, ИИ перестаёт предлагать',
'xfArchivedHint': 'В архиве: ИИ не предлагает, старые записи сохраняются',
'xfBoshqaFixedHint': '«Boshqa» — резервная папка: сюда попадает нераспознанное, не редактируется',
'xfFolderNoCatHint': 'Папка не найдена в списке категорий — проверьте интернет',
'xfMoveCap': 'ПЕРЕМЕСТИТЬ В ПАПКУ', 'xfMoveFrom': 'Сейчас: {cat} — куда переместить?',
'xfMoveNoCats': 'Нет других активных папок — проверьте интернет или создайте категорию',
'tIncomeFolderFixed': 'Папка дохода управляется системой',
'tFolderNoCat': 'Категория папки не найдена на сервере',
'tFolderRenamed': 'Папка переименована — записи перенесены',
'tFolderArchived': 'В архиве — ИИ больше не предлагает, история сохранена',
'tFolderUnarchived': 'Возвращено из архива — папка снова предлагается',
'tEntryNotFound': 'Запись не найдена — обновите и попробуйте ещё раз',
'tMovedTo': 'Перемещено: {cat}', 'logArchived': 'в архив', 'logUnarchived': 'из архива',

// lEn:
'xfEditFolderCap': 'EDIT FOLDER', 'xfRename': 'Rename',
'xfArchive': 'Archive', 'xfUnarchive': 'Unarchive', 'xfArchivedBadge': 'Archived',
'xfArchiveHint': 'Archiving isn\'t deleting: history is kept, AI stops suggesting it',
'xfArchivedHint': 'Archived: AI won\'t suggest it, old entries are kept',
'xfBoshqaFixedHint': '"Boshqa" is the fallback folder for unidentified entries — it can\'t be edited',
'xfFolderNoCatHint': 'Folder not found in the category list — check your connection',
'xfMoveCap': 'MOVE TO FOLDER', 'xfMoveFrom': 'Now: {cat} — where should it go?',
'xfMoveNoCats': 'No other active folders — check your connection or add a category',
'tIncomeFolderFixed': 'The income folder is managed by the system',
'tFolderNoCat': 'Folder category not found on the server',
'tFolderRenamed': 'Folder renamed — entries moved with it',
'tFolderArchived': 'Archived — AI won\'t suggest this folder, history kept',
'tFolderUnarchived': 'Unarchived — the folder is suggested again',
'tEntryNotFound': 'Entry not found — refresh and try again',
'tMovedTo': 'Moved to: {cat}', 'logArchived': 'archived', 'logUnarchived': 'unarchived',
```
(lEs/lFr/lZh uchun ingliz variantidan tarjima qiling — UI fallback tufayli bloklamaydi.)

### 4.2 store.dart — tafsilot qatoriga id (ko'chirish kartasi to'g'ridan-to'g'ri id ishlatsin)
Hozir UI desc+vaqt+summa bo'yicha topadi (ishlaydi), id bilan mustahkamroq. `_xarVals` ichida:
```dart
// ESKI:
            (xfGroups.last['rows'] as List).add({
              'desc': (e['note'] as String?)?.isNotEmpty == true ? e['note'] : e['cat'],
// YANGI:
            (xfGroups.last['rows'] as List).add({
              'id': e['id'], 'a': e['a'],
              'desc': (e['note'] as String?)?.isNotEmpty == true ? e['note'] : e['cat'],
```

### 4.3 store.dart — merge kirim↔chiqim guard (`_xfMergeAsk`)
```dart
// ESKI:
    if (hits.length < 2) {
      toast_(L()['tSayTwoFolders']);
      return;
    }
    set({'xfConfirm': {'kind': 'merge', 'from': hits[0]['f'], 'to': hits[1]['f']}});
// YANGI:
    if (hits.length < 2) {
      toast_(L()['tSayTwoFolders']);
      return;
    }
    final fromF = hits[0]['f'] as Map<String, dynamic>;
    final toF = hits[1]['f'] as Map<String, dynamic>;
    if (fromF['income'] != toF['income']) {
      toast_("Kirim va chiqim papkalari birlashtirilmaydi");
      return;
    }
    set({'xfConfirm': {'kind': 'merge', 'from': fromF, 'to': toF}});
```

### 4.4 api.dart — toza klient metodi (ixtiyoriy; UI hozircha o'z fallback'i bilan chaqiradi)
```dart
// ESKI:
  static Future<ApiRes> categories() => _req('GET', '/api/categories');
// YANGI:
  static Future<ApiRes> categories() => _req('GET', '/api/categories');
  // Papka tahriri: arxivlanganlar ham (archived flag bilan)
  static Future<ApiRes> categoriesAll() => _req('GET', '/api/categories?all=1');
```
(Qo'llangach xarajat.dart `_loadCats` ni `Api.categoriesAll()` ga o'tkazish mumkin — 3 qatorlik soddalash.)

### 4.5 src/services/parse.js — qoida-lug'atni mobil bilan sinxronlash (preview zaxirasi aniqroq)
```js
// ESKI:
const INC_NOUN = /\b(oylik|maosh|avans|daromad|bonus|kirim|foyda)\b|mijoz\w*|sotuv\w*/g;
const INC_VERB = /\boldim\b|\bsotdim\b|keldi|tushdi/g;
const EXP_NOUN = /kredit\w*|xarid\w*/g;
const EXP_VERB = /berdim|sarfladim|ishlatdim|to'ladim|toladim|ketdi|sotib\s+oldim/g;
// YANGI:
const INC_NOUN = /\b(oylik|maosh|avans|daromad|bonus|kirim|foyda|salary|income|profit|revenue)\b|mijoz\w*|sotuv\w*|ойлик|маош|даромад|кирим|фойда|аванс|бонус|зарплат[а-яё]*|доход[а-яё]*|мижоз[а-яё]*|сотув[а-яё]*/g;
const INC_VERB = /\boldim\b|\bsotdim\b|keldi|tushdi|qaytdi\b|qaytardi\b|\breceived\b|\bearned\b|\bgot\b|\bsold\b|олдим|сотдим|келди|тушди|қайтди|получил[а-яё]*|заработал[а-яё]*|пришл[а-яё]*|поступил[а-яё]*|продал[а-яё]*/g;
const EXP_NOUN = /kredit\w*|xarid\w*|\bqarzga\b|\brent\b|кредит[а-яё]*|аренд[а-яё]*|харид[а-яё]*/g;
const EXP_VERB = /berdim|sarfladim|ishlatdim|to'ladim|toladim|ketdi|sotib\s+oldim|qaytardim|qaytarib\s+berdim|\bspent\b|\bpaid\b|\bbought\b|\bgave\b|бердим|сарфладим|тўладим|туладим|кетди|сотиб\s+олдим|потратил[а-яё]*|купил[а-яё]*|заплатил[а-яё]*|оплатил[а-яё]*|отдал[а-яё]*/g;
```
(norm() matnni lowercase qiladi — /g yetarli. qaytardi\b anchori "qaytardim"ni kirimga yozib yubormaydi.)

### 4.6 XOTIRA-ovoz-va-kategoriya.md — §7 dagi "STT_ENABLED default false" jumlasini kodga moslab yangilash
(config.js hozir default TRUE — chat ovozli xabarlari uchun; Xarajat mic'i baribir kSttEnabled=false bilan yopiq.)

## 5. LIVE E2E REJASI (lead uchun)

Skript: `docs/team-reports/e2e/expenses.browser.js` — https://trust-backend-ft1s.onrender.com/health
tabida DevTools Console'ga qo'yiladi, `TOKEN = '<<PASTE_JWT>>'` almashtiriladi. 15 qadam, har biri
PASS/FAIL/WARN, oxirida console.table xulosa. TEST ma'lumotlari "TEST:" / "TEST Papka E2E" bilan belgilangan,
yozuvlar skript oxirida o'ziyoq o'chiriladi.

DIQQAT: 3, 10, 11, 12, 14-qadamlar YANGI backend kodi deploy qilinganini talab qiladi (aks holda WARN/FAIL —
skript buni izohlab beradi).

Fixture kerak emas (skript o'zi yaratadi). To'liq tozalash SQL (Supabase SQL editor, <UID> = test user id):
```sql
delete from public.expenses  where user_id = '<UID>' and (note ilike 'TEST:%' or raw_text ilike 'TEST:%');
delete from public.word_map  where user_id = '<UID>' and category like 'TEST Papka E2E%';
delete from public.corrections where user_id = '<UID>' and text ilike 'TEST:%';
delete from public.categories where user_id = '<UID>' and name like 'TEST Papka E2E%';
```

Mobil qo'lda tekshirish (flutter analyze + qurilma, lead/QA):
1. "kofe 25 ming" yozing — input summasi QIZIL, jo'natilgach chip qizil, papka raqami qizil sanaydi.
2. "oylik oldim 4 mln" — hammasi YASHIL. 3. Papka tafsiloti/jurnalda chiqim endi QIZIL (asosiy fix).
4. Papkani uzoq bosing → rename → grid/tafsilot/jurnal yangi nomda, eski yozuvlar joyida.
5. Arxivlash → tray chiplarida yo'qoladi, oy yozuvlari joyida; qayta uzoq bosish → «Arxivdan qaytarish».
6. Tafsilotda qatorni bosing → chips → boshqa papkaga ko'chadi (pulsi bilan), jurnalga tushadi.
7. Dark/light ikkalasida 1-6; oflayn rejimda karta xabarlari (toast + "internetni tekshiring") chiqadi.

## 6. O'ZGARGAN FAYLLAR + tekshiruv

| Fayl | O'zgarish | Tekshiruv |
|---|---|---|
| mobile/lib/screens/xarajat.dart | 3 ta rang fix; kalit-so'z sinxron; papka tahriri + ko'chirish UI (2 karta, long-press, row-tap); l10n fallback | qo'lda ko'zdan kechirish (sandboxda flutter yo'q) — **lead: `flutter analyze` majburiy** |
| src/routes/categories.js | ?all=1; rename KASKADI (expenses+word_map); Boshqa guardlari; 409 | `node --check` PASS |
| src/routes/expenses.js | ?category&limit; income-flip guard | `node --check` PASS |
| src/routes/stt.js | o'zgarmadi (ko'rib chiqildi — muammo yo'q) | — |
| docs/team-reports/e2e/expenses.browser.js | YANGI — live E2E skripti | `node --check` PASS |

Eslatma: sandbox bash mount'i eski snapshot (tahrirlar unda ko'rinmaydi) — `node --check` fayllarning
aynan nusxalarida bajarildi (~/chk). README API jadvalini yangilash lead integratsiyasida (categories ?all=1,
expenses ?category/limit).

## 7. CROSS-FINDINGS (boshqa agentlar hududi — tegilmadi)

1. **store.dart `_xfFolders()`**: papka yo'nalishi (income) birinchi yozuvdan olinadi — aralash papkada
   rang/summa noto'g'ri. Backend guardlarim aralashishni deyarli yopdi, lekin defensiv fix yaxshi bo'lardi:
   folder income'ni ko'pchilik yozuvdan yoki nom='Daromad' dan aniqlash.
2. **store.dart merge oqimi** N ta ketma-ket PATCH yuboradi (katta papkada sekin, yarim-holat riski) —
   kelajakda bulk endpoint (`PATCH /api/expenses/bulk-category`) yaxshiroq.
3. **store.dart `xfCfOk_` 'delf'**: papkani "o'chirish" = oy yozuvlarini butunlay DELETE qilish — XOTIRA
   §4 "o'chirish yo'q" ruhiga zid emas (bu toifa emas, yozuvlar), lekin undo YO'Q. Undo-toast qo'shish tavsiya.
4. **GET /api/expenses limitisiz** hamma yozuvni qaytaradi — 1-2 yillik faol user uchun og'irlashadi;
   hydrate'ga `?limit=` yoki oy-oralig'i qo'shishga tayyor turibdi (endpoint endi param oladi).
5. `_HlController._srvCache` statik va foydalanuvchiga bog'lanmagan — logout/login (boshqa akkaunt) da
   ham qoladi; preview user-neytral bo'lgani uchun xavfsiz, lekin bilib qo'yish kerak.

## 8. PRODUCTION RISKLAR

1. **Deploy tartibi:** mobil yangi UI eski backend bilan ham ishlaydi (?all=1 e'tiborsiz qoladi, rename
   kaskadsiz — papka bo'linadi!). Shuning uchun **avval backend deploy, keyin APK**.
2. Rename kaskadi tranzaksiyasiz (PostgREST) — categories yangilanib, expenses yangilanmay qolishi nazariy
   mumkin (keyingi rename yoki hydrate tuzatadi). Katta hajmda RPC/SQL funksiya afzal.
3. word_map merge N+1 so'rov — foydalanuvchi lug'ati kichik (yuzlab), muammo emas; global miqyosda RPC.
4. preview LLM har klaviatura pauzasida Groq chaqiradi (60/min IP limit bor) — Groq bepul kuni 2000 so'rov
   limiti bitta faol beta-userda ham yeyilishi mumkin. Monitoring (XOTIRA §5 4-bosqich) hali yo'q.
5. `_openMove` id-matcher (store patch 4.2 gacha): bir daqiqada bir xil desc+summa ikki yozuvdan birini
   tanlaydi — natija foydalanuvchi uchun farqsiz, lekin patch qo'llangani ma'qul.
6. Arxivlangan papka joriy oyda yozuvlari bo'lsa gridda ko'rinaveradi (balans rostligi uchun ongli qaror,
   toast tushuntiradi) — UX kuzatuvda qayta ko'rib chiqilsin.
