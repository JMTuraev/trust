# LIVE E2E + DEPLOY hisoboti — 2026-07-16 (RELEASE/QA)

**YAKUNIY HOLAT: BLOKLANDI — dashboard loginlari yo'q edi.**
Migratsiya, deploy va E2E **bajarilmadi**; production'ga hech qanday o'zgartirish kiritilmadi
(na SQL, na git push). Baza va servis avvalgi holatida (backend v3.2). Quyida nima tekshirildi,
nima topildi va davom ettirish tartibi.

---

## 1. Nima bajarildi (tayyorgarlik — muvaffaqiyatli)

| Qadam | Holat | Izoh |
|---|---|---|
| 4 E2E skript + sign-test-jwt.mjs o'qildi | ✅ | profile(31 qadam)/partners(28+)/expenses(15)/circles(43) — TOKEN placeholderlar aniqlandi |
| Fixture/cleanup SQL yig'ildi | ✅ | partners §4.2+§8.4, profile §9.1/9.3/9.4, expenses §5, circles §4/F3 |
| Migratsiya fayllari o'qildi | ✅ | `supabase/migrations/011_circles_hardening.sql`, `012_subscription_events.sql` — pastega tayyor |
| 4 ta test JWT imzolandi (lokal .env sir bilan) | ✅ | user 101 (trial) / 102 (premium) / 103 (expired) / 104 (warn) — faqat token chiqarildi, sir chop etilmadi |
| Prod health tekshiruvi | ✅ | `GET /health` → `{ok:true, service:'trust-backend', version:'3.2'}` — eski kod jonli (kutilganidek, deploy'dan oldin) |
| **JWT sir mosligi probe** | ⚠️ **MOS EMAS** | lokal imzolangan token bilan `GET /api/profile/me` → **401 "Token yaroqsiz"**. `render.yaml`da `APP_JWT_SECRET: generateValue: true` — prod siri Render tomonidan yaratilgan, lokal .env'dan farq qiladi. **Demak E2E uchun tokenlarni Render dashboard'dagi sir bilan (brauzer ichida, WebCrypto, sirni hech qayerga ko'chirmasdan) qayta imzolash SHART.** |

## 2. Blokator: login devorlari

- **Supabase** (`supabase.com/dashboard`) → `sign-in` sahifasiga yo'naltirdi; `api.supabase.com/platform/profile` (cookie bilan) → **401**. Sessiya yo'q.
- **Render** (`dashboard.render.com`) → `/login`. Sessiya yo'q.
- Qoidaga ko'ra kredensial kiritilmadi / OAuth bosilmadi. Foydalanuvchiga 3 marta xabar yuborildi
  (birinchi so'rov + 2 eslatma), **~55 daqiqa** kutildi (davriy tekshiruvlar bilan), oxirida
  Chrome kengaytmasi umuman uzildi (brauzer yopilgan). Javob kelmadi.

## 3. Nega deploy ham QILINMADI (ataylab)

Ketma-ketlik talabi: migratsiyalar deploy'dan OLDIN. `011_circles_hardening.sql` yurgizilmagan
holda v3.3 deploy qilinsa, dalilli doirani yopish (`status='closed'` soft-close) DB constraint'iga
uriladi → real foydalanuvchilarga 500. Shu sababli Supabase ochilmagach, `push_deploy.bat`
ataylab ishga tushirilmadi. `push_out.txt` yaratilmagan/yangilanmagan.

## 4. Bajarilmagan qadamlar

| Qadam | Holat |
|---|---|
| 011 + 012 migratsiyalari (Supabase SQL editor) | ❌ bajarilmadi |
| Fixture SQL (4 test user + partners/circles toʻplamlari) | ❌ bajarilmadi — **bazada hech narsa yaratilmadi** |
| Deploy (`push_deploy.bat`) + Render kuzatuvi + /health v3.3 | ❌ bajarilmadi |
| Prod sir bilan JWT (Render Environment → in-browser HS256) | ❌ bajarilmadi (Render login yo'q) |
| 4 E2E skript (profile → partners → expenses → circles) | ❌ yugurtirilmadi (0/4) |
| Cleanup SQL | — kerak emas (fixture yaratilmagani uchun baza toza) |

## 5. Topilmalar (keyingi yugurish uchun muhim)

1. **APP_JWT_SECRET lokal ≠ prod** — probe bilan isbotlandi (401, sof imzo bosqichida:
   `src/middleware/auth.js` DB'gacha yetmaydi). E2E'dan oldin fallback usul majburiy:
   Render → trust-backend → Environment → APP_JWT_SECRET'ni reveal qilib, o'sha sahifada
   javascript orqali DOM'dan o'qib, WebCrypto bilan 4 token imzolash (sir chatga/faylga/bashga chiqmaydi).
   Claimlar: `sub=<uuid>`, `phone=<raqamlar + siz>`, `role:'authenticated'`, `aud:'authenticated'`, `iat`, `exp=now+7200`, HS256.
2. **SUB_GRACE_DAYS prod render.yaml'da yo'q** → default 0 — expired 402 testlari to'g'ri ishlashi kutiladi.
3. **Circles hisobotidagi fixture SQL'ni ishlatmang** (`111.../222.../333...` UUID + shu telefonlar):
   profile §9.1 fixture'lari bilan telefon UNIQUE to'qnashuvi bo'ladi. Circles skriptiga 101/102/103
   userlari to'liq mos (owner trial / user2 premium / expired). Circles hisobotidagi
   `premium_until = now()+10 years` update'ini HAM ishlatmang — 101 trial bo'lib qolishi shart (profile 1.1–1.5).
4. **Skript tartibi va oraliq SQL:** profile skripti oxirida 101'ni soft-delete qiladi —
   partners'dan OLDIN §9.3 tiklash SQL'i yurgiziladi:
   `update public.profiles set deleted_at=null, updated_at=now() where id='11111111-1111-4111-8111-111111111101';`
5. Token muddati 2 soat — jarayon cho'zilsa qayta imzolash kerak bo'ladi.

## 6. Davom ettirish tartibi (login bo'lgach, aynan shu ketma-ketlik)

1. Supabase SQL editor: `011_circles_hardening.sql` → `012_subscription_events.sql` (fayllardan aynan).
2. Old-tekshiruv: `select id, phone, full_name from public.profiles where phone in ('998900000001','998900000002','998900000003','998900000004');` — bo'sh/faqat test bo'lsin.
3. Fixture SQL: profile §9.1 (4 user), so'ng partners §4.2 nom-updatelari + §8.4 (103 created_at -30d).
4. `push_deploy.bat` (File Explorer, double-click) → `push_out.txt` → Render deploy → `/health` v3.3.
5. JWT: avval lokal probe; 401 bo'lsa (kutiladi) Render Environment fallback (5-band, 1-topilma).
6. E2E: profile → (§9.3 tiklash SQL) → partners → expenses (TOKEN=101) → circles (101/102/103).
7. Cleanup: expenses SQL (UID=101) → partners §4.4+§8.4 → circles (faqat `TEST E2E%` doira/notif) → profile §9.4 → SELECT tekshiruvlar.
8. Ushbu faylni natijalar bilan yangilash.

## 7. Menyu bo'yicha production xulosa

Hech bir menyu jonli tekshirilmadi — **xulosa yo'q**. v3.2 jonli, v3.3 chiqarilmadi;
foydalanuvchilarga ta'sir yo'q, regressiya xavfi yo'q (hech narsa o'zgartirilmadi).

---

## 8. 2-yugurish (davomi) — 2026-07-16, keyinroq

**HOLAT: yana BLOKLANDI — brauzer tanlovi tasdiqlanmadi.**

- Hisob(akkaunt)ga **ikkita** Chrome kengaytmasi ulangan chiqdi (ikkalasi ham shu kompyuterda, Windows):
  `Browser 1` (27a9085b-…) va `Browser 2` (2e91d26c-…). Qaysi birida Supabase/Render sessiyalari
  borligini agent o'zi tanlashi mumkin emas (MCP xavfsizlik qoidasi).
- Foydalanuvchiga chatda xabar yuborildi + har ikkala kengaytmaga **2 marta** "Connect" tasdiqlash
  oynasi yuborildi (~4–5 daqiqa kutildi) — javob kelmadi.
- Qoidaga muvofiq (≤5 daqiqa kutish) yugurish to'xtatildi. **Hech narsa o'zgartirilmadi**:
  012 migratsiya yurgizilmadi, fixture yo'q, push_deploy.bat bosilmadi, E2E 0/4.
- 011 oldingi yugurishda muvaffaqiyatli qo'llangan (Supabase'da mavjud) — davomida 012'dan boshlanadi.
- Davom ettirish: foydalanuvchi to'g'ri Chrome oynasida "Connect" bosishi (yoki chatda
  Browser 1/2 ni aytishi) bilan §6 tartibi 012-migratsiyadan davom etadi.
